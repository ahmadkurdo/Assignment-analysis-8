

1. Run the program by typing the following command ---> python3 assignment.py
2. Login as a super administrator: username --> Super
				   password --> Super123@


3. All the input validation is done by the input handler class
4. Encryption and decryption is done by the encryptor class
5. All the data is encrypted and saved to the data.json file
6. Each user role is a object we used dependency injection to validate if a user is logged in
7. Each user will be redirected to its own screen based on what role it has.
8. All the data handeling such as registering an system administrator or client is done database class

Sorry for this short documentation, we just came to know that a documentation was needed. But everything works fine except for the loggin part.

